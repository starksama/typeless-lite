# Optional template values for local development workflows.
# The current MVP reads settings from the in-app settings UI.

OPENAI_API_KEY=
OPENAI_API_BASE_URL=https://api.openai.com/v1
WHISPER_MODEL=whisper-1
FORMAT_MODEL=gpt-4o-mini
HOTKEY=Cmd+Shift+Space
FORMAT_PROMPT=You are a concise writing assistant. Clean up the transcript for grammar and punctuation while preserving intent. Return only final text.
